'use client';
import { Zap, Activity, LayoutGrid, Link2, type LucideIcon } from 'lucide-react';

export const methodIcons: Record<string, LucideIcon> = {
  bolt: Zap,
  surge: Activity,
  grid: LayoutGrid,
  fuse: Link2,
};
